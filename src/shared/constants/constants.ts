// Theme names are declared in shared/styles/_themes.scss and need to match
export const THEME_NAMES = {
  LIGHT: "light",
  DARK: "dark",
};

export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
  SIGNUP: "/signup",
};
